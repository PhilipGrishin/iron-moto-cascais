# C8 Report: Project Family Generator Migration

Date: 2026-07-31

## Scope And Source Decision

- Status: **confirmed** against repository head `affa9121` before migration.
- All 11 indexable project slugs are now registered in `PROJECT_CONFIGS` and
  rendered in four languages by `build_project_pages.py`.
- Fighter keeps its approved Markdown source and structured renderer.
- The 10 former static projects use
  `content/projects/legacy_projects_4lang.json`. This file was mechanically
  extracted from the 40 published language variants before implementation;
  it preserves their localized `<main>` markup, visible copy, links, image
  paths, dimensions and metadata without editorial rewriting.
- Project pages are no longer generated by `build_i18n.py`; no generated
  project HTML contains `window.ICM_I18N_PAGE`.
- The obsolete `enhance_project_pages.py` post-processor was removed because
  its existing visible output is now part of the maintained project data.

## Redirect Decision

The two historical aliases are now generated as noindex redirects in all four
languages:

- `nezlamniy` -> `unbreakable`
- `quanta` -> `quanta-r`

This resolves the confirmed hard 404 at `/ru/projects/nezlamniy/` and gives
every supported language a same-language redirect target. Six localized files
were added; the two English files retain their existing behavior. Redirects
remain outside `sitemap.xml`, so the indexable URL inventory and all sitemap
bytes remain unchanged.

## Visible Text And Media Comparison

Method: parse `<main>` from every 44 pre-migration pages at `affa9121`, collapse
whitespace, hash the UTF-8 rendered text with SHA-256, then repeat after the
complete project workflow. The same comparison also hashed the ordered main
DOM structure and every source/img media descriptor. Total compared visible
text: 213,007 UTF-8 bytes.

Each cell below is the first 12 characters of the identical before/after text
hash. Full hashes are stored with the maintained source data and enforced by
`validate_project_pages.py`.

| Project | EN | RU | UK | PT |
| --- | --- | --- | --- | --- |
| inspirium | `85b7a346d9c6` | `c7aa9e63400b` | `6564ba7df11f` | `c74aa0c2e914` |
| beckman | `32f6a508eaab` | `63a4db69c618` | `021acace3f26` | `aec32043c6bd` |
| unbreakable | `a4755335d600` | `4b78f26cc4e7` | `9162242ab619` | `e356e817fa24` |
| quanta-r | `4e91c4edd143` | `1fd33450f2e3` | `41c8a2ecbfef` | `d0cce5b78bce` |
| burly | `91a481f17713` | `52b35e792060` | `fb53f732f652` | `c513a309d319` |
| sturmvogel | `b7f87672ea39` | `31df03346f71` | `dd9878e4ea72` | `6ff272736860` |
| geometric | `ea7e4f8c0577` | `41c95ff765b5` | `e60d1de16e76` | `b9c93c9a8ba2` |
| joker | `008b6a2bf57e` | `75a1db065622` | `dfcf906c83bc` | `e426c38a679c` |
| hellboy | `6e4f4c6023d8` | `7d748ff1ca86` | `cb687d771da0` | `7179305a32c8` |
| true-religion | `921491e734b1` | `43e79332c865` | `0be2990b945b` | `ef56b30584a0` |
| fighter | `3a105c2135ba` | `725b26e49048` | `d5e061959c0d` | `3f8cc1b58b14` |

Result: 44/44 text hashes matched, 44/44 main-structure hashes matched,
and 44/44 media hashes matched. Differences are confined to head/schema,
removal of translation attributes/inline payloads and HTML serialization.

## Schema And Delivery Contract

- Every project emits the same type set: `Article`, `WebPage`, `ImageObject`,
  `LocalBusiness`, `PostalAddress`, `BreadcrumbList` and `ListItem`.
- `CreativeWork`, year-only `dateCreated` and inline `Organization` stubs are
  absent.
- `datePublished` is the real first Git publication timestamp for migrated
  pages; `dateModified` preserves the already-published per-language sitemap
  content timestamp. All values are full ISO-8601 with timezone.
- Article author and publisher are `@id` references to the complete
  `LocalBusiness` entity, which includes `name`, `url`, `image`, `telephone`,
  `priceRange`, postal address and a dimensioned logo.
- Each page has one responsive AVIF preload matching the hero source, exactly
  one high-priority hero image and no high-priority lazy image.
- The migrated hero AVIF/WebP/JPEG sets and every gallery descriptor are
  byte-for-byte path-identical to the pre-migration markup. Fighter retains its
  registered AVIF/WebP media set.
- All four asset references are derived from `CACHE_BUST`; main assets remain
  `20260724a` and project assets remain `20260710b`.

## Immutable Scope

- Changed indexable HTML: 44 project detail variants.
- Added noindex HTML: 6 localized redirect stubs.
- Changed non-project HTML: zero across the other 168 sitemap URLs.
- `assets/main.css`, `assets/main.js`, `assets/projects.css` and
  `assets/projects.js`: unchanged.
- `sitemap.xml` SHA-256 before and after:
  `4910de2803fdd535c37198cf27ed541c23e66be8bd53afe80466881261e54971`.
- All 212 sitemap lastmod values are therefore byte-identical.
- Repeating the complete project workflow produced the same tracked project
  HTML diff SHA-256
  `b4d1bb88ede5076515012e6c5890c394cb47857174e20d362c1aba91d38f2667`.

## Verification Boundary

The repository validator checks the Google Article required publisher/name/
logo contract and reference resolution. External Rich Results Test and final
production checks are recorded after publication; local validation must not be
described as an external Google UI result.

## Local Verification

- Implementation commit: `ce25a7c2`.
- The complete canonical generator sequence from `scripts/build/README.md`
  completed in a clean clone of that commit and left empty
  `git status --short`.
- All four repository validator groups passed: SEO (212 URLs), brand pages
  (7 sets), Harley Hub (12 pages), and project pages (all 11 slugs across all
  four languages).
- The rebuilt sitemap retained SHA-256
  `4910de2803fdd535c37198cf27ed541c23e66be8bd53afe80466881261e54971`.
- The implementation diff passed `git diff --check`; Python modules compiled
  successfully with bytecode redirected outside the repository, and the
  maintained JavaScript files passed `node --check`.
