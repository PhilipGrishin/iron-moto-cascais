# P-THE-FIRST Delivery Report

Date: 2026-08-04 (Europe/Lisbon)

## Outcome

The First is published as the fourteenth registered project through the common
project-page pipeline. The implementation commit is `06e04fff810b7ea6cc79244ea0635ec6f105c439`.
GitHub Pages workflow `30883611614` completed successfully.

Public URLs:

- `https://ironcustommotors.com/projects/the-first/`
- `https://ironcustommotors.com/pt/projects/the-first/`
- `https://ironcustommotors.com/ru/projects/the-first/`
- `https://ironcustommotors.com/uk/projects/the-first/`

Cache-bypass production requests returned HTTP 200 for all four URLs.

## Maintained Sources And Generated Scope

The approved four-language source was copied without rewriting to
`content/projects/the-first_4lang.md`. The supplied file and repository copy
have the same SHA-256:

```text
c92296e9b521059fb06fe0b2d5957ddd7d6a8987d25b9fb2fd59099e169bf44d
```

Service notes and `[VERIFY]` markers are not rendered. The common Markdown
project parser, exact-copy validator and normalized visible-text hashes protect
the four generated pages.

Implementation scope:

- 4 new project HTML pages.
- 220 existing indexable HTML pages regenerated because The First was added to
  the shared desktop and mobile project navigation.
- Exactly 20 existing `<main>` elements changed: the four `/projects/`, four
  `/custom/`, four `/harley-custom/`, four Cocktail and four Fetish pages.
- The remaining 200 existing HTML changes are project-menu chrome only.
- 75 new project hero/gallery media files plus 9 optimized Harley portfolio
  variants, all generated from the supplied JPEG sources.
- Project Markdown/data registries, `sitemap.xml` and generated `llms.txt`.
- No CSS, JavaScript or cache-bust value changed.

The First is last in `PROJECT_TILES`, after Cocktail and Fetish. The card uses
year 2012 and the exact approved localized category and location metadata.

## Integrations And Reciprocal Links

The First is present in the project listing, Custom page and Harley Custom Hub
in every language. Desktop and mobile navigation derive from the same
`PROJECT_TILES` registry and expose the localized URL on all 224 sitemap pages.

The required reciprocal content links are present inside `<main>` on all eight
source pages:

- `/projects/cocktail/` and `/projects/fetish/`
- `/pt/projects/cocktail/` and `/pt/projects/fetish/`
- `/ru/projects/cocktail/` and `/ru/projects/fetish/`
- `/uk/projects/cocktail/` and `/uk/projects/fetish/`

The First source already links back to Cocktail and Fetish in every language.
A separate repository crawl checked 29,944 internal link references across all
224 sitemap pages and found zero broken targets.

## Metadata And Structured Data

Every language page has a self-canonical URL and the mutual `en`, `pt-PT`,
`ru`, `uk` and `x-default` hreflang cluster. The generated graph uses the
standard project types, including `Article`, `WebPage`, `ImageObject`,
`BreadcrumbList` and the complete `LocalBusiness` entity. Article publisher
and author resolve through `https://ironcustommotors.com/#business`; publication
and modification values are full ISO-8601 timestamps with timezone.

Local schema validation passed. Google Rich Results Test UI was not required
for this task and was not run; this is not represented as external RRT evidence.

## Responsive Media And Network Evidence

The hero has AVIF/WebP sources and JPEG fallback at 800, 1600 and 2400 px. Its
AVIF preload has the same `imagesrcset` and `imagesizes="100vw"` contract as
the rendered source. The static candidate audit resolves as follows:

```text
390px/DPR3: target=1170px -> the-first-1600.avif
390px/DPR2: target=780px -> the-first-800.avif
768px/DPR2: target=1536px -> the-first-1600.avif
1280px/DPR1: target=1280px -> the-first-1600.avif
1440px/DPR1: target=1440px -> the-first-1600.avif
```

Local browser evidence at 390 x 844 / DPR2 recorded one request for
`the-first-800.avif`. At 1440 px / DPR1 it recorded one request for
`the-first-1600.avif`. The production page-assets inventory at 390 x 844
contained exactly one The First hero resource, `the-first-800.avif`. Both
layouts had exactly one `fetchpriority="high"`, no high-priority lazy image and
zero horizontal overflow. All 11 gallery images are dimensioned and lazy.

## Sitemap Discipline

The sitemap grew from 220 to 224 URLs. No URL was removed. The production
cache-bypass sitemap is byte-identical to the repository file, SHA-256:

```text
5908ba0946f97dcd31567142364eef68db0086eda0b666fd847df38ba3b13e00
```

Added lastmod entries:

- `/projects/the-first/`
- `/pt/projects/the-first/`
- `/ru/projects/the-first/`
- `/uk/projects/the-first/`

Moved lastmod entries, limited to visible content changes:

- `/projects/`, `/pt/projects/`, `/ru/projects/`, `/uk/projects/`
- `/custom/`, `/pt/custom/`, `/ru/custom/`, `/uk/custom/`
- `/harley-custom/`, `/pt/harley-custom/`, `/ru/harley-custom/`,
  `/uk/harley-custom/`
- `/projects/cocktail/`, `/pt/projects/cocktail/`,
  `/ru/projects/cocktail/`, `/uk/projects/cocktail/`
- `/projects/fetish/`, `/pt/projects/fetish/`, `/ru/projects/fetish/`,
  `/uk/projects/fetish/`

All other 200 pre-existing sitemap lastmod values are byte-identical to the
previous version. Shared navigation alone did not move a date.

## Validator And Rebuild Evidence

Broad validator:

```text
SEO validation passed: 224 sitemap URL(s)
```

Brand validator:

```text
Brand page validation passed: 7 brand page set(s).
```

Harley Hub validator:

```text
Harley Hub validation passed: 12 pages and all required integrations
```

Project validator group:

```text
OK: beckman project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: burly project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: cocktail project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: fetish project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: fighter project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: geometric project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: hellboy project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: inspirium project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: joker project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: quanta-r project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: sturmvogel project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: the-first project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: true-religion project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
OK: unbreakable project page passed multilingual copy, media, schema, cache-bust, redirect and integration checks
```

The complete documented Full Safe Rebuild was run in a clean clone of
`06e04fff`. It regenerated all maintained HTML, all four pricing PDFs,
`sitemap.xml` and `llms.txt`, repeated all four validator groups, and left:

```text
CLEAN_CLONE_STATUS_BEGIN
CLEAN_CLONE_STATUS_END
```

## Open Factual Boundary

The supplied source does not verify The First's current whereabouts or a
Motobike 2012 award/result. The published page makes neither claim. This
non-blocking follow-up is recorded in `docs/OPEN_TASKS.md`.
