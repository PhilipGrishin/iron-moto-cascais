name = "icm-leads"
main = "worker.mjs"
compatibility_date = "2026-08-24"

# Create the namespace first, then replace this placeholder before deployment:
#   npx wrangler kv namespace create LEAD_COUNTS
[[kv_namespaces]]
binding = "LEAD_COUNTS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

# STATS_TOKEN is intentionally absent from this file. Set it with:
#   npx wrangler secret put STATS_TOKEN
